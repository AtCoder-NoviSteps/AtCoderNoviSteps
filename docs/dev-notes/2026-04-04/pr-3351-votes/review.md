# PR #3351 レビュー評価メモ

## AI レビュー指摘の妥当性

### Critical

| #   | 指摘                                               | 判断    | 判断軸                                                                                                                |
| --- | -------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------- |
| 1   | SVG arc path — 100% セグメントが描画されない       | ✅ 妥当 | SVG 仕様上、始点と終点が同一座標の場合 `arc` コマンドが省略される既知制約。デュアルアークによる回避が標準解           |
| 2   | ナビバーラベルの不整合（"投票" vs "グレード投票"） | ✅ 妥当 | パンくず (`/votes/[slug]/+page.svelte`) で "グレード投票" を確認済み。不整合があれば E2E テストが実害を持って失敗する |

### Major

| #   | 指摘                                                                             | 判断    | 判断軸                                                                                                                                                    |
| --- | -------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 3   | ドーナツリングの半径計算（`OUTER_RADIUS` → `(OUTER_RADIUS + INNER_RADIUS) / 2`） | ✅ 妥当 | stroke-width 付き `<circle>` のリング中心線は `(外径 + 内径) / 2` が正。`OUTER_RADIUS` のままだと外縁が意図したサイズをはみ出す                           |
| 4   | PENDING フォールバックロジックがルートスクリプトに存在                           | ✅ 妥当 | AGENTS.md / coding-style.md が「ルートは redirect/error のみ、表示ロジックは utils/」と明示。プロジェクトルール違反                                       |
| 5   | フラスコアイコンのツールチップがキーボード非対応                                 | ✅ 妥当 | `tabindex` / `role` / `aria-label` のない `<span>` はキーボード操作不可。Web 標準。実装方針は Flowbite 既存コンポーネントで代替できないか先に確認すること |

### Minor

| #   | 指摘                                              | 判断        | 判断軸                                                                                                                                                                                                          |
| --- | ------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6   | 外部リンクに `rel="noopener noreferrer"` が未設定 | ✅ 妥当     | `noopener`: `window.opener` 経由の親タブ乗っ取りを防止（セキュリティ実害あり）。`noreferrer`: Referer ヘッダーを抑制しURLに含まれる情報の漏洩を防止。コストがほぼゼロなので外部リンクに機械的に付与するのが標準 |
| 7   | 仮グレードロジックの重複                          | ✅ 妥当     | AGENTS.md が DRY を明示。ただし「2箇所の条件が本当に同一か」をコードで確認した上で抽出すること                                                                                                                  |
| 8   | ドキュメント coverage 80% 未達                    | ⚠️ 条件付き | coding-style.md に「exported 関数全件に TSDoc」の要件はある。ただし「80%」という閾値の出所が不明（自動チェックが設定済みか確認必要）                                                                            |

---

## 目視確認による追加指摘

### UI 調整（votes ページ）

1. **文字サイズ**: h1 と検索フィールドの placeholder 以外を一回り大きくしたい
2. **グレードアイコン**: サイズが小さい → `workbooks/[slug]` ページと同じサイズに揃える
3. **フラスコアイコンの位置**: 一覧表のコンテスト別表示で、グレードの右ではなく左に表示する
